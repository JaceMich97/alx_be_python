# Define the current age variable that requests user input of how they are
current_age = int(input("how old are you?"))

# calculate the future age of the user in 2050 assuming the current year is 2023( subract the users current age from 27 years)
years_to_add = 27
future_age = current_age + years_to_add

print(f"In 2050, you will be {future_age}")

